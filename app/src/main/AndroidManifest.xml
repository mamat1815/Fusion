<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/fusion_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/fusion_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Fusion"
        tools:targetApi="34">
        <activity
            android:name=".ui.mentor.MentorActivity"
            android:exported="false" />
        <activity
            android:name=".ui.detailteam.DetailTeamActivity"
            android:exported="false" />
        <activity
            android:name=".ui.detailpost.DetailPostActivity"
            android:exported="false" />
        <activity
            android:name=".ui.main.MainActivity"
            android:exported="false" />
        <activity
            android:name=".ui.signin.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>